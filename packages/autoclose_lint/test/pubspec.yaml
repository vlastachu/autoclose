name: example
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  autoclose:
  autoclose_flutter:

dependency_overrides:
  autoclose:
    path: ../../..
  autoclose_flutter:
    path: ../../../packages/autoclose_flutter
  autoclose_lint:
    path: ../../../packages/autoclose_lint

dev_dependencies:
  autoclose_lint:
  lints: ^2.0.0
  custom_lint: ^0.5.3
  flutter_lints: ^2.0.0

flutter:

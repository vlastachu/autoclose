name: autoclose_flutter
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '^3.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  autoclose:

dependency_overrides:
  autoclose:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
  vm_service: ^11.9.0
  mockito: ^5.4.0

flutter:
